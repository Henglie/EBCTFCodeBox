# 更新日志

## v0.1.2（未发布）

### 🆕 新增算法（全部通过官方向量验证后接线）
- **CityHash**（`cityhash`）— Google 非加密高速哈希，CityHash32/64。经 Crypto++ `city-test.cc` 官方 299 组向量全量对拍通过（含 1MB 大输入），并修复了此前实现中的四处字节序/移位/变量错位缺陷。
- **Threefish**（`threefish`）— Skein v1.3 内建可调分组密码，256/512/1024 位分组。Crypto++ threefish.txt 官方向量全部通过。
- **Skipjack**（`skipjack`）— NSA 1998 解密分组密码。NIST SP800-17 Table 6 全部 11 组官方向量（加解密双向）通过。
- **MARS**（`mars`）— IBM AES 决赛圈分组密码。Crypto++ marsval.dat 全部 10 组向量通过（1999-08 修订版密钥编排）。

### ⚡ 一键解码（智能解码）
- **暴力爆破独立通道**：XOR/凯撒/哈希字典/彩虹表/HMAC/PBE/Playfair/ZIP/CRC32/bkcrack 等 11 个爆破算法独立成池，勾选后单独归组运行展示，不参与主排序（避免报告文本被误判置顶）。
- **解析层数 1~3 可调**：多层链式解码深度由用户显式选择（原仅随强度档隐式变化）。
- **宽松判定模式（增强/极强/最强/自定义档）**：只判断输入字符种类数，不判定具体字符——变体编码题（如「喵呜」表 0/1、emoji 表二进制）也能让相关算法参与解码；默认/快速档保持严格定义域识别不变。
- **参与算法列表改为双列**，空间利用率翻倍；强度滑块刻度位置按「默认左2 / 增强左1 / 极强右1 / 最强右2」微调。

### 🛠 修复
- **BrainFuck 括号宽容**：孤儿 `]`/多余 `[` 不再报「括号不匹配」，按 NOP 处理（对齐常见 BF 解释器行为）。
- **本地桥 CORS 拦截**：桥服务 CORS 白名单从写死的 `localhost:8180` 改为按请求 Origin 反射，仅放行本机来源（localhost/127.0.0.1/::1 任意端口），外部站点仍拒绝——127.0.0.1 打开页面时本地桥不再失效。
- **MARS worker 侧注册缺失**：`registerAll.js` 漏注册导致一键解码静默少该算法，已补。
- **xxHash 重复导入**清理。

### 🎨 UI
- 本地桥分类一级菜单改用主题色高亮，二级菜单文字恢复原色（仅 EXE 徽章保留橙色区分）。
- 左侧导航多分类同时展开、二级菜单整块深色填充、字体与间距收紧。

## v0.1.1（已发布）
- 518 op 智能解码 + 插件/MCP 体系 + 18 语言 i18n（详见 README）。
